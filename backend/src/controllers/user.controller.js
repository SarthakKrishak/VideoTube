import { asyncHandler } from "../utils/asyncHandler.js";


const registerUser = asyncHandler(async (req, res) => {
    // Credentials
    // see if any user exists with those credentials
    // if not then register it
})

export {registerUser}